#include "main.h"


/**
 * handle_flags - handle flags if any
 * @flag_struct: structure of flags to handle
 * @format: non-custom conversion specifier
 * Return: number of charaters printed to stdout
 */


int handle_flags(FLAGS flag_struct, const char *format);
